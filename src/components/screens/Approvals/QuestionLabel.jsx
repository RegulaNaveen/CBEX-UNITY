import React from 'react';
import Typography from 'apollo-react/components/Typography';

const QuestionLabel = ({ questionLabel }) => {
  return <Typography>{questionLabel}</Typography>;
};

export default QuestionLabel;
