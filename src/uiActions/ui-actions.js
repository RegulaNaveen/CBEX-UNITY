import { Subject } from 'rxjs';

export const UI_ACTION = {
  openGenerateModal: 'openGenerateModal'
};
export const actionChannel = new Subject();
export const actionChannel = new Subject();
export const actionChannel = new Subject();